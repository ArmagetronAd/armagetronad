image: docker:19.03.0

only:
  - master
  - legacy_0.2.8
  - legacy_0.2.8.3

services:
  - docker:19.03.0-dind

variables:
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: ""

stages:
  - build

build_image:
  stage: build
  script:
    - docker build .
